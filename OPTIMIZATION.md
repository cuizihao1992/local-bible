# 优化体检记录

## 本轮已处理

- 前端 API 响应解析更稳：接口返回纯文本错误时也能显示可读提示。
- 多选复制的 `selectionchange` 事件增加帧级节流，减少长章节拖选时的重复 DOM 扫描。
- 清理旧的每节工具条渲染死代码，降低维护成本。
- 导入个人数据时优化 SQLite 事务处理，失败会回滚，历史记录在导入连接关闭后再写入。
- 桌面版加入单实例保护，重复打开 exe 时聚焦已有窗口。
- `npm run check` 覆盖桌面入口文件。
- 锁文件补齐依赖来源与完整性信息，提升 Electron 打包可复现性。

## 后续可继续优化

- 将过大的 `server.js` 拆成数据源、用户数据、API 路由、静态服务几个模块。
- 将过大的 `static/app.js` 拆成状态、阅读器、搜索、个人资料、词典、桌面交互几个模块。
- 增加前端轻量测试脚本，覆盖快速定位、多选复制、侧栏折叠等核心交互。
- 给桌面版补应用图标和安装版 target。
- 研究注释/辞典中疑似加密或压缩库的解析方式。
- 搜索增加分页或索引，避免大范围搜索时一次性结果过多。
