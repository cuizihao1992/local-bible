# Codex 圣经阅读器开发工程

工程目录：`D:\codex-bible-reader`。

基于原 Codex 工程 1.9.70，保留完整 Git 历史。参考 `D:\bible-reader` 的分步选择、前端模块化和跨平台 API 检查设计。原工程与参考工程均保留。

## 本地运行

需要 Node.js 24 或更新兼容版本。在 PowerShell 执行：

```powershell
cd D:\codex-bible-reader
.\start.ps1
```

打开 http://127.0.0.1:8767 。经文数据库继续使用 `D:\bibleDownload`，个人数据保存在本工程 `data` 目录。原工程个人数据没有自动覆盖进来，需要时可从旧版导出、在新版导入。浏览器设置按站点保存，新端口需重新配置。

```powershell
npm run check
$env:BIBLE_READER_URL = 'http://127.0.0.1:8767'
npm test
```

Android 源码已包含在工程中，本轮发布版本为 1.9.71。当前 `.tools` 和 `node_modules` 通过本机目录链接复用原工程工具及依赖；构建产物放在 D 盘本工程 `dist/android`。沿用原签名覆盖安装旧 APK。迁移至其他电脑时需重新准备依赖、JDK、SDK、Gradle 与原签名文件。

设计原则：参考 `D:\bible-reader` 的思路，不照搬布局或实现。以真实阅读、查找、笔记流程评估合理性；保留顺手的功能，重新设计不合理的操作，逐步验证再发布。

## 本轮完成

- 书卷、章节、经节分步展示，支持返回上一步。
- 关闭选书面板恢复原阅读位置；确认经节后才提交选择。
- 过期的选节请求不再覆盖新请求结果。
- 面板打开期间屏蔽正文左右键翻章。
- 书卷面板标题固定，启用移动端安全区。
- 修复 Windows CRLF 换行导致的前端源码 smoke 断言误报。

## 后续优化计划

1. 阅读交互：把选书草稿与正文状态彻底分离，统一面板焦点、返回键与手势行为。
2. 手机和平板：横屏、软键盘、超大字号和多选工具栏专项检查。
3. 笔记收藏：增加筛选、自动保存草稿和导入冲突预览。
4. 工程质量：参考项目的模块划分，逐步拆分 app.js，建立 Node/Android API 行为契约测试。
5. 原文资料：核验现有 orig 数据与公开来源，补齐可下载的希伯来文、希腊文资源及许可说明。
6. 发布：完成 Android 真机回归后统一调整版本号、更新说明和 APK 发布。

后续每批修改本地 Git 提交。完成项与计划项分开记录，不以源码字符串断言代替真实交互测试。
