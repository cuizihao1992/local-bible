# 版本计划

## V1.9.57 移动端样式与交互统一优化
目标：让菜单、弹层、分享面板和底部导航更像手机 App，减少遮挡、滚动和触控不适。
已完成：

- 移动端阅读设置、目录、搜索、Strong、词典、AI、我的、更新说明、分享图等内容面板统一为底部弹层
- 弹层打开时自动显示遮罩并锁定底层滚动
- 弹层顶部关闭栏固定，长内容滚动时仍可关闭
- 经文菜单“更多”区域在手机端跨整行显示，按钮网格更稳定
- 底部导航缩短“上章 / 下章”标签，降低小屏拥挤
- 按钮增加按下反馈，焦点状态更清晰
- smoke test 增加 sheet、遮罩、滚动锁定、触控反馈等断言

## V1.9.56 经文图片分享
目标：让收藏、转发经文更方便，长按经文后可以直接生成适合手机分享的图片。
已完成：

- 经文菜单“更多”新增“分享图”
- 新增经文图片预览面板和 `shareCanvas`
- Canvas 生成竖版 PNG，经文自动换行、缩小并避免溢出
- 支持系统分享、复制图片和保存图片，能力不足时降级为下载或复制文字
- 返回键、遮罩关闭和内容面板互斥流程支持关闭分享面板
- smoke test 增加分享图入口、Canvas、按钮和核心函数断言

## V1.9.55 语音跳转书卷确认
目标：降低语音识别听错书卷后直接跳错位置的概率。
已完成：

- 新增语音书卷确认面板
- 以斯、腓利、约书、帖撒、提摩、彼得、哥林等易混口令会弹出候选书卷
- 选择候选后保留原识别出的章 / 节并继续跳转
- 返回键、遮罩关闭和内容面板互斥流程支持关闭确认面板
- smoke test 增加确认面板、候选动作和歧义检测断言

## V1.9.54 搜索范围偏好保持
目标：让常用搜索范围可自动保留，减少重复选择“本卷 / 新约 / 全本”的操作。
已完成：

- 新增 `searchScope` 本地偏好
- 启动时恢复上次选择的搜索范围
- 搜索范围下拉框变化时立即保存
- 普通搜索和最近搜索都使用已保存范围
- 恢复时只接受 `all`、`ot`、`nt`、`book` 合法范围值
- smoke test 增加搜索范围持久化断言

## V1.9.53 最近搜索快捷入口
目标：提升高频查经体验，让常查关键词可以一键复用。
已完成：

- 搜索框下方新增最近搜索区域
- 非引用跳转的关键词搜索会自动保存最近 8 条
- 点击最近搜索会回填搜索框，并按当前搜索范围重新搜索
- 空状态自动隐藏，避免占用阅读栏空间
- smoke test 增加最近搜索状态、渲染、点击和样式断言

## V1.9.52 阅读字体与页边距
目标：让阅读设置更接近精读类应用，用户可以按设备和习惯调整正文气质与留白。
已完成：

- 阅读设置新增“正文字体”：无衬线 / 衬线
- 阅读设置新增“页边距”滑块，并显示具体 px 数值
- 字体和页边距写入本地偏好，下次打开自动恢复
- 正文、对照经文、注释正文和“我的”经文摘要统一使用阅读字体
- 手机端页边距跟随设置，同时保留最小安全边距
- smoke test 增加阅读字体和页边距断言

## V1.9.51 我的列表显示经文摘要
目标：让收藏、高亮、笔记列表不只显示出处和备注，而是直接看到经文内容，回顾时更快识别。
已完成：

- `/api/user/marks/all` 返回 `text` 经文正文摘要
- “我的”结果卡片显示正文摘要
- Android 离线后台同步为 marks/all 增加正文摘要
- 摘要文本使用阅读字体和独立层级，和标签/笔记区分
- smoke test 增加 Node、前端和 Android 摘要断言

## V1.9.50 书卷面板选到经节
目标：参考 `D:\bible-reader` 的三级选择体验，让点击当前书卷名后不仅能选书卷和章节，也能继续选到具体经节。
已完成：

- 书卷选择面板新增经节选择区域
- 点章节后先展示该章节号，不再立刻关闭面板
- 点具体节号会关闭面板并跳转到该节
- 新增“返回章节”和“从第 1 节读”两个快捷操作
- 手机端选节面板增加换行布局，避免按钮拥挤
- smoke test 增加经节选择面板和跳转路径断言

## V1.9.49 我的筛选状态保持
目标：让“我的”面板在高亮、收藏、笔记之间筛选后，再用标签搜索时不丢失当前分类。
已完成：

- 新增 `currentMyFilter` 保存当前“我的”分类
- “我的”筛选按钮选中态改为跟随 `currentMyFilter`
- 标签输入框按 Enter 时沿用当前分类重新查询
- `/api/user/marks/all` 请求统一使用当前筛选状态
- smoke test 增加当前筛选状态和标签搜索保持分类断言

## V1.9.48 我的高亮筛选
目标：让“我的”面板把高亮经文作为一等资料类型，方便回顾已标记重点的经文。
已完成：

- `/api/user/marks/all` 新增 `kind=highlight` 过滤
- “我的”面板新增“高亮”筛选按钮
- 个人资料仪表盘增加高亮数量统计
- “我的”筛选按钮增加当前选中态
- “我的”空状态和加载文案覆盖收藏、高亮、笔记
- smoke test 增加高亮筛选 API、入口、统计和选中态断言

## V1.9.47 朗读时阅读栏保持可控
目标：避免朗读跟随滚动时，手机端顶部阅读栏自动隐藏，导致用户不方便停止朗读。
已完成：

- 朗读状态加入 `body.speaking`
- 朗读期间 `updateReadingChromeVisibility()` 不再自动隐藏阅读栏
- 开始朗读时主动唤出阅读栏并短暂固定
- 朗读期间顶部“停止”按钮保持可见且更醒目
- smoke test 增加朗读时阅读栏保持显示的断言

## V1.9.46 系统朗读与经节跟随
目标：让离线 APK 即使没有本地 MP3，也能用系统 TTS 朗读本章，并在朗读过程中跟随高亮当前经节。
已完成：

- 顶部阅读栏新增“朗读/停止”按钮
- 音频面板新增系统朗读控制、朗读速度和关闭按钮
- 网页端使用 `speechSynthesis` 作为系统朗读兜底
- Android 新增 `TtsBridge`，通过 `AndroidTtsApi` 调用系统中文 TTS
- 朗读过程中当前经节自动高亮并滚动到可见区域
- 切换章节时会停止当前朗读，避免旧章节声音继续播放
- smoke test 增加朗读按钮、Android TTS bridge 和经节跟随断言

## V1.9.45 经文菜单与引用跳转
目标：参考 `D:\bible-reader` 的经文菜单体验，让常用动作更清爽，并让注释/AI 输出里的经文引用可直接跳转。
已完成：

- 经文右键/长按菜单默认只展示收藏、高亮、笔记、复制、多选
- 查词、注释、AI 解释、AI 上下文、AI 笔记收进“更多”区域
- 新增 `linkVerseRefs()`，将注释和 AI 回答中的书卷引用渲染为可点击链接
- 新增引用链接点击处理，可直接跳转到对应书卷、章节和经节
- smoke test 增加经文菜单更多区域和引用链接断言

## V1.9.44 资源包下载失败恢复
目标：让资源包下载轮询失败或停止后，按钮状态可恢复，用户可以直接重试或清理缓存。
已完成：

- 新增资源包按钮恢复 helper
- 资源包下载轮询 `error/cleared` 停止时恢复按钮并显示可重试提示
- 资源包下载同步异常和轮询停止共用恢复逻辑
- 下载完成后的刷新流程用 `finally` 释放 busy 状态
- smoke test 增加资源包失败恢复断言

## V1.9.43 章节加载状态收尾
目标：让连续翻章、慢请求或旧请求过期时，上一章/下一章和章节按钮的 loading 状态恢复更可靠。
已完成：

- `loadChapter()` 改为 `finally` 统一恢复 `chapterLoading`
- 只有当前章节请求 token 才能恢复按钮状态，避免旧请求覆盖新请求
- 章节加载成功、失败和中途返回都走统一收尾判断
- smoke test 增加当前 token `finally` 恢复断言

## V1.9.42 章节切换清理旧面板
目标：避免上一章/下一章后保留旧搜索、词典、AI、我的记录等内容面板，让当前页面状态更可信。
已完成：

- `resetVerseInteraction()` 统一关闭内容类面板
- 上一章/下一章、书卷章节跳转、搜索结果跳转和语音跳转共用清理逻辑
- 避免旧章节的搜索、词典、AI 结果或我的记录残留在新章节下方
- smoke test 增加章节切换清理旧面板断言

## V1.9.41 阅读进度保存反馈
目标：让手机端“标记/已读”保存过程有明确反馈，避免连续点击和失败后状态不清。
已完成：

- 底部“标记/已读”保存中显示“保存中”
- 保存中禁用底部标记按钮和仪表盘标记按钮
- 重复触发时提示“正在保存阅读进度，请稍候”
- 保存失败时显示错误提示并恢复按钮文字
- smoke test 增加阅读进度保存 busy 断言

## V1.9.40 小屏多选复制布局
目标：让 420px 以下手机多选复制浮条不挤压、不遮挡，复制按钮更稳定可点。
已完成：

- 超窄屏多选浮条改为两列弹性布局
- 已选摘要独占一行
- 复制格式下拉框独占一行
- 复制/取消按钮各占一列并限制溢出
- smoke test 增加小屏多选复制布局断言

## V1.9.39 横滑翻章可靠性
目标：减少 Android WebView 或旧设备上左右滑触发 `pointercancel/touchcancel` 后不翻章的问题。
已完成：

- 横滑手势记录最后一次触点坐标
- `pointercancel` 时如果已经形成有效横滑，也会完成上一章/下一章
- 旧设备 `touchcancel` fallback 同步完成有效横滑
- 横滑阈值从 76px 降到 54px，方向比例从 1.45 降到 1.2，更贴近手机自然手势
- smoke test 增加移动端横滑取消保护断言

## V1.9.38 小标题兜底与可见性
目标：解决无标题译本和手机端看不到小标题的问题，让每章标题来源清楚可见。
已完成：

- 本地 JS 后台在当前译本无 `Titles` 时自动借用已安装的标题译本
- Android 离线后台同步返回参考小标题和标题来源
- 前端区分“真实小标题 / 参考小标题 / 内置小标题”
- 正文小标题摘要和段落标题取消过深缩进，手机端更容易看到
- smoke test 增加 KJV 参考小标题来源断言
- 顶部侧栏关闭按钮和章节选择后关闭侧栏都改为调用 `closeSidebar()`
- 避免直接 `classList.remove("sidebarOpen")` 绕过阅读工具栏保持显示逻辑


## V0.1 本地基础阅读器

目标：先把本地经文读起来。

已完成：

- JS 后台
- 读取 `D:\bibleDownload\bibles`
- 版本列表
- 书卷/章节选择
- 经文逐节显示
- 上一章/下一章
- 移动端侧栏
- 记住上次阅读位置
- 基础 API 健康检查

## V0.2 多版本对照

目标：支持各译本对照阅读。

已完成：

- 增加对照版本选择
- 每节显示主版本和对照版本
- 支持 1-3 个对照版本
- 自动处理缺章、缺节
- 记住上次对照版本选择
- 增加多版本章节 API

## V0.3 搜索与快速定位

目标：快速找到经文。

已完成：

- 输入 `约3:16`、`创1:1` 直接跳转
- 关键词搜索当前译本
- 搜索结果显示书卷、章节、节号、摘要
- 点击结果跳转
- 支持搜索范围：全本、旧约、新约、当前书卷

## V0.4 注释联动

目标：从阅读器升级成查经工具。

已完成：

- 读取 `D:\bibleDownload\cj`
- 当前章节显示可用注释
- 点击某节查看注释
- 支持切换注释来源
- 图片注释先显示占位，后续再处理图片渲染

## V0.5 原文与 Strong 编号

目标：支持原文研读。

已完成：

- 保留或开关 Strong 编号显示
- 点击 Strong 编号查看释义
- 联动 `D:\bibleDownload\orig\cbol.db`
- 显示原文、音译、词义、出现位置

## V0.6 笔记、收藏、高亮

目标：变成个人阅读工具。

已完成：

- 本地新建用户数据库
- 收藏经文
- 高亮经文
- 经文笔记
- 标签分类
- 最近阅读历史

## V0.7 音频与朗读

目标：支持听读。

已完成：

- 扫描 `D:\bibleDownload\ld`
- 当前章节匹配 mp3
- 播放、暂停、进度条
- 自动下一章

## V0.8 辞典/百科

目标：补充查词和背景资料。

已完成：

- 读取 `D:\bibleDownload\cd`
- 搜索百科/辞典词条
- 经文关键词可打开词条
- 支持图片资料显示

## V0.9 设置与体验优化

目标：让阅读更舒服。

已完成：

- 夜间模式
- 字号、行距设置
- 简繁显示偏好
- 复制经文格式
- 键盘快捷键
- 手机端细节优化

## V1.0 稳定本地版

目标：可长期使用。

已完成：

- 错误提示完善
- 数据库兼容性检查
- 首页：最近阅读、收藏、搜索入口
- 导入/导出个人笔记
- 打包启动脚本
- 完整使用说明

## V1.1 交互与移动端优化

目标：让常用操作更顺手，手机阅读更接近 App 体验。

已完成：

- 经文右键弹出操作菜单
- 移动端长按经文弹出操作菜单
- 菜单支持收藏、高亮、笔记、复制、查词、注释
- 移动端底部固定导航栏
- 移动端侧栏宽度和底部留白优化

## V1.2 收藏笔记与搜索优化

目标：让个人资料和搜索结果更好用。

已完成：

- 收藏/笔记统一管理面板
- 按收藏、笔记、标签筛选
- 从管理面板点击跳回经文
- 搜索结果关键词高亮
- 后台提供全部标注查询接口

## V1.3 工程化与回归验证

目标：给后续优化留一条稳定护栏。

已完成：

- 新增 `npm test`
- 增加 API smoke test
- 覆盖健康检查、章节、搜索、Strong、诊断、标注接口

## V1.4 阅读进度与长期计划

目标：让本地版可以长期跟踪读经进度。

已完成：

- 后台新增章节阅读进度表
- 支持按译本记录已读章节
- 顶部显示当前译本阅读进度
- 当前章支持标记已读/取消已读
- 章节目录显示已读状态
- 首页增加阅读进度卡片
- 支持继续跳转到下一章未读章节
- 导入/导出包含阅读进度
- smoke test 覆盖阅读进度接口

## V1.5 阅读界面与配色优化

目标：继续向微读圣经靠近，让正文阅读更干净，外观更可调。

已完成：

- 经文正文取消每节常驻收藏/高亮/笔记/复制工具条
- 保留桌面右键和移动端长按弹出操作菜单
- 设置区新增配色预设选择
- 新增经典纸张、清润青绿、安静蓝灰、柔和玫瑰、简洁墨白五套预设
- 配色预设支持日间/夜间两种主题
- 已读章节、顶部栏、移动端底栏、搜索与笔记区域跟随配色变量

## V1.5.1 手机端兼容修复

目标：让窄屏手机上的导航、搜索、正文和弹层不互相挤压。

已完成：

- 手机端顶部栏隐藏重复的上一章/下一章按钮
- 搜索栏在手机端独占一行并收紧控件宽度
- 侧栏改用 `100dvh` 并适配安全区底部
- 打开侧栏时隐藏底部导航
- 正文、注释和面板增加底部安全留白，避免被底部导航遮挡
- 长按经文菜单避开底部导航
- 420px 以下超窄屏增加专门压缩规则

## V1.5.2 多选复制优化

目标：让鼠标多选多节经文后可以方便复制，不再只能复制单节。

已完成：

- 识别正文区域内跨节文本选区
- 选中经文后显示“复制所选”浮条
- 复制内容按“书名 章:节 经文”逐节换行
- 复制时只取主译本经文正文，不混入 Strong、对照版本和笔记
- 手机端浮条避开底部导航

## V1.5.3 简洁侧栏优化

目标：左侧面板默认只展示最常用的阅读入口，高级配置需要点击后才展示。

已完成：

- 默认侧栏只展示译本、书卷、章节
- 对照版本、注释、Strong、音频、辞典、主题、导入导出移入“高级设置”
- 高级设置默认折叠
- 使用原生 `details/summary`，不增加额外脚本
- 补充折叠按钮样式，让入口更清晰

## V1.6.0 Windows exe 桌面版

目标：把本地网页应用打包成可双击运行的 Windows `.exe`。

已完成：

- 新增 Electron 桌面入口
- 桌面版启动时自动寻找空闲端口
- 桌面版自动启动本地 Node 后台
- 窗口加载内部本地服务地址
- 关闭桌面窗口时自动结束后台进程
- 桌面版用户数据保存到 Windows AppData
- 新增 `npm run desktop`
- 新增 `npm run dist:win`
- 已生成 Windows 便携版 exe

## V1.6.1 稳定性与维护性优化

目标：检查现有实现中的风险点，先收一批高收益优化。

已完成：

- 前端 API 响应解析支持纯文本错误，提示更稳定
- 多选复制选区监听增加帧级节流
- 清理未使用的每节工具条渲染函数
- 导入个人数据时优化 SQLite 事务和回滚
- 桌面版增加单实例保护
- `npm run check` 覆盖 Electron 桌面入口
- 新增 [OPTIMIZATION.md](./OPTIMIZATION.md) 记录体检结果和后续优化清单

## V1.7.0 Android 离线 APK

目标：做出不依赖电脑后台服务的 Android 离线 APK。

已完成：

- 新增原生 Android WebView 工程
- APK 内置前端静态资源
- APK 构建时打包 `D:\bibleDownload\bibles` 下 26 个经文译本
- Android 原生代码拦截 `/api/...` 请求并本地返回 JSON
- 支持离线译本列表、书卷、章节、多译本对照和搜索
- 支持 Android 本机用户数据库、阅读历史、阅读进度、收藏、高亮、笔记、导入和导出
- 新增 `npm run dist:android`
- 生成 `dist\android\local-bible-reader-offline-1.7.0-debug.apk`

## V1.7.1 Android 离线 release APK

目标：生成更适合安装分发的离线 APK，并保留 debug 构建入口。

已完成：

- 将 Android 版本升级到 `1.7.1`
- `npm run dist:android` 默认构建 release APK
- 新增 `npm run dist:android:debug` 保留调试包构建
- 自动在 `.tools` 下生成本地 release 签名文件，不提交密钥
- 生成 `dist\android\local-bible-reader-offline-1.7.1-release.apk`

## V1.8.0 Android 独立 APK 重做

目标：让 APK 作为独立手机 App 运行，安装后不再读取电脑 D 盘，也不再依赖网页服务。

已完成：

- 将 Android 版本升级到 `1.8.0`
- 前端静态资源在 APK 中使用相对路径加载，避免 `file:///android_asset` 下脚本和样式加载失败
- 新增 Android 原生 `getJson` 桥接，前端 GET/POST 接口都可直接调用 APK 内本地数据层
- APK 运行界面移除 `D:\bibleDownload` 文案
- APK manifest 设置应用图标和圆形图标
- 构建任务声明静态网页和经文 DB 输入，前端变更会重新打进 APK
- 新增 `npm run verify:android` 检查 APK 包名、版本、图标、签名、26 个内置译本和独立运行资源
- 生成 `dist\android\local-bible-reader-offline-1.8.0-release.apk`

## V1.9.0 Android 轻量 APK 与按需资源包

目标：让 APK 更小，只内置常用译本，其他译本和注释库从 GitHub Release 按需下载。

已完成：

- Android 版本升级到 `1.9.0`
- APK 只内置 `和合本.db`、`和合本修订版.db`、`KJV.db`、`WEB.db`
- 新增 Android 网络权限和资源包下载/解压/安装接口
- 新增 `/api/packages` 和 APK 原生 `installPackage` 桥接
- 新增“高级设置 -> 离线资源”面板
- 下载更多译本后自动刷新译本和对照列表
- 下载注释库后自动刷新注释列表，并支持读取基础 `commentary` 表内容
- 新增 `npm run dist:android:packages`
- 生成 `bibles-extra-v1.9.0.zip` 和 `commentaries-v1.9.0.zip`
- `npm run verify:android` 改为验证 4 个内置译本
- APK 从约 316 MB 降到约 43 MB

后续：

- 将辞典、原文和音频继续做成可选离线数据包

## V1.9.1 Android 更新与返回手势优化

目标：让 APK 可以在应用内检查 GitHub Release 更新，并修复系统返回手势在菜单打开时直接退出的问题。

已完成：

- Android 版本升级到 `1.9.1`
- 系统返回键/返回手势优先交给网页处理，菜单、设置、搜索、我的、版本说明等面板打开时只关闭当前面板
- 新增 Android 原生 `AndroidUpdateApi` 桥接
- 新增“版本更新”入口，可检查 GitHub 最新 Release
- 新增 APK 下载并调起系统安装器
- 新增“版本更新说明”页面，记录本地版本更新内容和 GitHub Release 说明
- 数据资源包下载地址同步到 `v1.9.1`

发版要求：

- 每次推送 GitHub Release 前，同步更新 `package.json`、`package-lock.json`、`android/app/build.gradle`、`scripts/build-android.ps1`、`scripts/verify-android-apk.ps1` 中的版本号
- Release tag、APK 文件名、资源包文件名和 Android 内置下载地址必须使用同一个版本号

## V1.9.2 书卷选择面板与更新策略

目标：让移动端选择书卷更快，并保证应用内检查更新可以发现新 APK。

已完成：

- Android 版本升级到 `1.9.2`
- 书卷选择增加搜索框
- 书卷选择增加“全部 / 旧约 / 新约”筛选
- 书卷从系统下拉框扩展为按钮网格，显示简称、约别和章节数
- 章节面板显示当前书卷、总章数和当前章
- 保留原生下拉选择，兼容桌面和键盘操作

发版提醒：

- 覆盖同一个 GitHub Release asset 只能替换下载文件，应用内检查更新不会发现版本变化
- 需要让 App 检查到更新时，必须升级版本号并创建新的 Release tag

## V1.9.3 下载体验与语音按钮优化

目标：让移动端语音入口更醒目，并让 APK 下载资源包/更新包时有可见进度和缓存清理能力。

已完成：

- Android 版本升级到 `1.9.3`
- 语音按钮使用独立强调样式，区别于普通导航按钮
- 资源包下载增加百分比、已下载大小和总大小显示
- APK 更新下载增加百分比、已下载大小和总大小显示
- 下载失败后可以清理资源包临时缓存和 APK 更新缓存
- Android 下载改为后台线程，前端通过状态轮询更新进度

## V1.9.4 MiMo 与 AI 配置检查

目标：让语音识别服务支持小米 MiMo 配置，并能检查当前 AI Key/模型是否可用。

已完成：

- Android 版本升级到 `1.9.4`
- AI 配置增加小米 MiMo Provider
- 新增 MiMo Key、MiMo 文本模型和 `mimo-v2.5-asr` 语音模型配置
- 新增“检查 AI”按钮，对当前文本模型 Provider 发起轻量请求验证配置
- DeepSeek 增加默认模型和思考模式配置，后续在 V1.9.6 校准为 `deepseek-v4-flash` / `deepseek-v4-pro`
- 语音服务切换到 MiMo 时自动选择 `mimo-v2.5-asr`

## V1.9.6 AI 模型校准与能力入口说明

目标：把 AI 配置里的模型名和触发方式校准到当前可用方案，避免语音按钮误导用户。
已完成：

- DeepSeek 文本模型更新为 `deepseek-v4-flash` / `deepseek-v4-pro`
- 已保存的旧 `deepseek-chat` / `deepseek-reasoner` 配置会自动迁移到新模型名
- 小米语音识别模型说明为 `mimo-v2.5-asr`
- OpenAI 语音识别推荐 `gpt-4o-mini-transcribe`，可选 `gpt-4o-transcribe` / `whisper-1`
- “能做什么”说明补充 AI 功能入口：语音跳转、经文解释/问答、笔记整理、主题查经、搜索意图解析
- 云端语音按钮提示改为当前状态说明：已能保存配置，但录音上传仍需后续接入

## V1.9.7 经文菜单 AI 查经

目标：把 AI 能力放到经文长按/右键菜单里，先完成可用的文本查经入口。
已完成：

- 经文菜单增加“AI 解释”“AI 上下文”“AI 笔记”
- 新增 AI 查经结果面板
- AI 输出支持一键复制
- 复用 AI 配置里的 DeepSeek / OpenAI / 小米 MiMo 文本模型
- 根据回答风格自动控制输出长度

## V1.9.8 APK 下载链接

目标：让用户检查更新后可以自己选择下载 APK，或复制 APK 链接到浏览器/聊天工具中使用。
已完成：

- 版本更新区增加“下载最新 APK”按钮
- 增加“复制 APK 链接”按钮
- 检查更新后不再自动下载，改为显示最新 APK 状态并启用操作按钮
- Android APK 内继续使用原生下载并拉起安装
- 普通本地网页可直接从 GitHub Release 查询最新版本并打开 APK 链接

## V1.9.9 小米 MiMo 云端语音

目标：让选择小米 MiMo 语音服务后，底部语音按钮可以真正录音上传识别，并让“检查 AI”覆盖语音模型配置。
已完成：

- “检查 AI”同时报告文本模型与当前语音服务配置
- 选择 MiMo 语音时检查 `mimo-v2.5-asr` 是否正确
- Android 原生层新增云端录音 Bridge
- 按住底部语音按钮录音，松开后上传到小米 MiMo ASR
- MiMo 返回文本后复用原有语音跳转解析

## V1.9.10 MiMo 鉴权配置

目标：处理小米 MiMo 返回 `Invalid API Key` 时难以排查的问题，支持不同 Key 类型需要的不同地址。
已完成：

- AI 配置增加“小米 MiMo Base URL”
- MiMo 文本检查和语音上传都使用可配置 Base URL
- MiMo 请求同时发送 `Authorization: Bearer ...` 和 `api-key`
- MiMo 401 错误提示补充 Key、空格和 Token Plan 专属 Base URL 排查方向

## V1.9.11 MiMo Token Plan 地址兼容

目标：适配小米 MiMo Token Plan / CodePlan 后台显示的 `/v1` Base URL，避免用户必须填写完整接口路径。
已完成：

- `小米 MiMo Base URL` 支持填写 `https://.../v1`
- 检查 AI 与语音上传会自动拼接 `/chat/completions`
- 如果已填写完整 `/chat/completions` 地址则保持原样

## V1.9.12 MiMo Key 类型切换

目标：同时支持普通 MiMo Key 和 CodePlan/Token Plan Key，避免两种 Key 混用地址导致检查失败。
已完成：

- AI 配置增加“小米 MiMo Key 类型”
- 普通 Key 固定使用默认 Base URL
- CodePlan / Token Plan 使用手动填写的专属 Base URL
- “检查 AI”会显示当前 MiMo 类型和实际测试地址
- 语音上传按同一类型与地址规则执行

## V1.9.37 我的面板读取反馈
目标：让“我的收藏与笔记”打开和筛选时有即时反馈，避免连续点击造成重复请求。
已完成：

- 增加 `myPanelLoading`，防止重复读取我的面板内容
- 我的面板读取中显示“正在读取我的收藏与笔记...”
- 读取中设置 `aria-busy` 并禁用筛选按钮
- 重复触发时提示“正在读取我的内容，请稍候”
- 关闭面板或请求完成后恢复 busy 状态
- smoke test 增加我的面板 busy、筛选禁用和读取反馈断言

## V1.9.36 经文跳转忙碌保护
目标：让搜索结果、Strong 出现位置、我的记录、语音和快捷输入等多入口跳转不互相打架。
已完成：

- 增加 `referenceJumpInProgress`，统一保护经文跳转流程
- 重复触发跳转时提示“正在跳转经文，请稍候”
- 跳转成功、失败或章节加载异常后都会恢复跳转状态
- smoke test 增加经文跳转忙碌状态和重复触发反馈断言

## V1.9.35 AI 结果复制反馈
目标：让 AI 结果复制这种高频操作有清晰状态，避免连续点击和失败后按钮文案停住。
已完成：

- AI 结果复制增加 `aiCopyInProgress`，防止重复复制
- 复制中按钮显示“复制中”并暂时禁用
- 复制成功后提示“已复制 AI 结果”，按钮恢复“复制结果”
- 复制失败或关闭 AI 面板后恢复复制状态
- AI 复制按钮增加 disabled 视觉样式
- smoke test 增加 AI 复制忙碌状态和恢复断言

## V1.9.34 个人数据导入导出反馈
目标：让个人数据导入导出这种高价值操作有清晰状态，避免重复点击和静默等待。
已完成：

- 导出增加 `exportInProgress`，防止连续点击重复生成文件
- 导出中按钮显示“导出中”，完成后提示“数据导出完成”
- 导入重复触发时提示“正在导入数据，请稍候”
- 导入中按钮显示“导入中”，完成或失败后恢复按钮状态
- 数据按钮增加 disabled 视觉样式
- smoke test 增加导入导出忙碌反馈断言

## V1.9.33 阅读工具栏保持显示
目标：让手机阅读时关闭弹层后可以立刻继续翻章或调整设置，减少工具栏突然隐藏。
已完成：

- 关闭侧栏、目录、阅读设置、经文菜单、多选栏和内容面板后短暂保持工具栏可见
- 新增 `readingChromePinnedUntil` 和 `keepReadingChromeVisible()`，避免刚关闭弹层就隐藏上下工具栏
- Android 资源包构建改为流式 `ZipArchive`，避免大注释包触发 `Compress-Archive` 内存溢出
- smoke test 增加工具栏保持显示和流式资源包构建断言

## V1.9.32 多选复制稳定性
目标：让手机阅读时的多选复制更顺手，减少重复复制和残留文字选区。
已完成：

- 多选复制增加 `selectionCopyInProgress`，防止重复复制
- 复制中禁用格式选择和复制按钮，按钮显示“复制中/已复制”
- 复制完成或取消选择时清理系统文字选区
- smoke test 增加多选复制忙碌状态和清理选区断言

## V1.9.31 语音按钮触摸兼容与状态保护
目标：让手机端按住语音按钮在旧 WebView/旧平板上更稳定，减少重复触发造成的状态错乱。
已完成：

- 增加 `voiceInputActive`，防止语音识别重复开始
- 增加 `voiceStopPending`，防止松手、取消、离开按钮时重复停止
- 缺少 `PointerEvent` 的旧 WebView 使用 `touchstart/touchend/touchcancel` 兜底
- 语音结果、错误、取消和缺少 stop 接口时统一恢复按钮状态
- smoke test 增加语音触摸兜底与防重复断言

## V1.9.30 标注保存反馈与防重复
目标：让收藏、高亮、保存笔记这些高频经文操作有明确反馈，并避免同一节经文重复写入。
已完成：

- 收藏、高亮、保存笔记增加同一节经文保存防重复保护
- 重复保存时提示“正在保存标注，请稍候”
- 收藏和高亮成功后显示轻提示
- 保存笔记按钮显示“保存中/已保存”
- 保存笔记重复点击返回后会恢复按钮文字
- smoke test 增加标注保存反馈与防重复断言
- 扫描确认本地 26 个经文译本中 7 个包含真实 `Titles` 小标题数据
- 版本选择下拉增加“有小标题/无小标题”状态
- 正文顶部显示“真实小标题/内置小标题/当前无小标题”
- 当前译本无小标题时提示可切换到有标题数据的版本
- smoke test 增加小标题状态提示断言

## V1.9.29 章节导航忙碌保护
目标：章节读取期间减少连续点击、滑动和键盘触发带来的状态抖动，让翻章行为更确定。
已完成：

- 新增 `chapterLoading` 状态
- 章节读取期间禁用上一章、下一章和底部翻章按钮
- 书卷选择面板中的章节按钮在加载中暂时禁用
- 滑动或键盘在章节读取中触发翻章时显示“正在读取经文，请稍候”
- smoke test 增加章节导航忙碌状态断言

## V1.9.28 章节加载失败可重试
目标：章节读取遇到临时失败时给用户明确恢复入口，减少卡在错误页的挫败感。
已完成：

- 章节加载失败页显示当前章节和错误原因
- 失败页增加“重试”按钮
- 点击重试会重新读取当前章节并回到顶部
- 章节读取失败时显示可恢复状态提示
- smoke test 增加章节失败重试断言

## V1.9.27 搜索工具忙碌反馈
目标：让搜索、加载更多和词典查询在等待响应时有清楚的按钮状态，减少重复点击和不确定感。
已完成：

- 搜索面板增加 `aria-busy` 状态
- 新搜索请求中搜索按钮显示“搜索中”
- 搜索“加载更多”请求中按钮禁用并显示“加载中”
- 词典查询请求中禁用“查”按钮并显示“查找中”
- 关闭搜索或词典面板时自动恢复按钮状态
- smoke test 增加搜索和词典忙碌反馈断言

## V1.9.26 异步旧结果保护
目标：避免搜索、词典、Strong、AI、我的等异步请求在快速切换操作后用旧结果覆盖当前界面。
已完成：

- 搜索、词典、Strong、AI、我的面板增加请求序号
- 新关键词搜索可打断旧搜索，旧搜索返回后不再覆盖新结果
- 搜索“加载更多”继续保留防重复点击保护
- 主动关闭面板时会让对应未完成请求失效
- smoke test 增加异步旧结果保护断言

## V1.9.25 内容面板互斥与关闭逻辑
目标：让搜索、词典、Strong、AI、我的、更新说明、书卷选择和阅读设置的打开/关闭行为更像成熟 App，避免多个面板叠加。
已完成：

- 新增统一 `closeContentPanels()`，集中关闭搜索、Strong、词典、AI、我的、更新说明
- 打开侧栏、书卷选择、阅读设置时自动收起其它内容面板
- 打开搜索、词典、Strong、我的、AI 结果时保持单一内容面板视图
- 阅读设置打开时同时收起经文菜单和多选栏，减少底部空间冲突
- smoke test 增加内容面板互斥断言

## V1.9.24 旧设备触摸手势兼容
目标：提升旧平板、旧 WebView 上阅读区左右滑切章和长按经文菜单的稳定性。
已完成：

- 阅读区滑动切章抽出统一手势判断，PointerEvent 和 TouchEvent 共用同一套阈值
- 缺少 `PointerEvent` 的旧 WebView 使用 `touchstart/touchmove/touchend/touchcancel` 兜底
- 旧触摸环境下长按经文仍可打开经文菜单
- 手指移动超过阈值时自动取消长按，避免滑动和长按误触
- smoke test 增加旧触摸兜底结构断言

## V1.9.23 小标题可见性与搜索分页
目标：确认小标题数据链路，并让手机端正文里的段落标题更容易被看到，同时保护大范围搜索性能。
已完成：

- 确认内置和合本、和合本修订版 APK 数据包包含真实 `Titles` 小标题数据
- 正文小标题改为带节号的醒目段落标题，移动端取消过深左缩进
- 搜索 API 增加 `offset`、`nextOffset`、`hasMore` 分页元数据
- 前端搜索结果增加“加载更多”，避免一次性渲染过多结果
- Android 离线接口同步支持搜索分页
- smoke test 和 APK verify 增加小标题显示与搜索分页断言

## V1.9.22 按钮状态与防重复提交

目标：修复手机端边界按钮状态、数据写入和下载入口的重复触发问题。
已完成：

- 底部上一章/下一章按钮同步第一章/最后一章 disabled 状态
- 移动端 disabled 按钮增加视觉样式
- 标记已读/取消已读增加忙碌保护，避免重复写入
- 导入数据增加忙碌保护、成功/失败提示，并在结束后清空文件选择
- 资源包安装增加全局忙碌保护，避免多个资源包同时下载
- 更新检查和 APK 下载增加忙碌保护
- 下载轮询异常、失败或清理状态会释放忙碌标记
- smoke test 增加按钮状态与防重复提交静态断言

## V1.9.21 阅读加载稳定性

目标：修复连续滑动、连续点下一章等快速操作下可能出现的旧请求覆盖新章节问题。
已完成：

- 章节加载增加 `chapterLoadToken`，旧请求返回后不再渲染
- 经文、标记、进度、注释、音频使用同一次导航的章节快照加载
- 旧请求不会写入阅读历史或保存当前状态
- 遮罩点击统一走返回关闭逻辑
- 轻提示增加 `role=status` 与 `aria-live=polite`
- 语音按钮状态点改为 CSS 绘制，减少旧 WebView 字体差异
- smoke test 增加章节加载防陈旧与可访问性静态断言

## V1.9.20 阅读操作反馈与状态复位

目标：继续按商用 App 手感优化阅读页，让翻章、复制、跳转、返回这些高频动作都有明确反馈且不残留旧状态。
已完成：

- 滑动到第一章/最后一章时显示边界提示
- 单节复制、多选复制、复制 APK 链接、清理下载缓存增加轻提示
- APK 下载失败时提示可复制链接到浏览器下载
- 跳转经文时自动收起搜索、我的、词典、原文、AI、经文菜单和多选栏
- 切章、切书、切版本统一清理目标节、经文菜单、多选栏等经文级临时状态
- 返回键优先关闭经文菜单和多选栏，再关闭大面板
- smoke test 增加阅读交互关键函数静态断言

## V1.9.19 小标题可见性与诊断

目标：把经文数据库中的小标题状态直接暴露给用户，并让本章小标题在阅读页更容易发现。
已完成：

- `/api/versions` 返回每个译本的 `titleCount`
- Android 离线接口返回每个内置译本的 `titleCount`
- 阅读页顶部显示当前译本是否包含小标题数据
- 正文顶部增加“本章小标题”快捷列表，可点击跳到对应经节
- 正文内继续按节显示真实小标题
- smoke test 固定验证和合本腓利门书 1 章 4 条小标题

## V1.9.18 移动端阅读交互

目标：按商用 App 标准梳理阅读页关键交互，优先修复左右滑不能切章的问题。
已完成：

- 正文区域左右滑切换上一章/下一章
- 菜单、书卷选择、阅读设置、搜索结果、经文菜单、多选等打开时禁用滑动切章，避免误触
- 明显滑动时取消长按经文菜单，避免滑动和长按冲突
- 手机端下滑阅读时隐藏顶部栏和底部栏，上滑或点正文恢复
- 增加旧 WebView 的 `color-mix()` 样式兜底
- smoke test 增加真实小标题数据链路检查

## V1.9.17 真实小标题数据接入

目标：确认并使用经文数据库自带小标题，不再只依赖前端内置兜底标题。
已完成：

- 扫描确认部分中文译本含 `Titles(Book, Chapter, Verse, Scripture)` 表
- Node 本地后台读取 `Titles` 并返回到章节 API
- Android 离线后台读取 `Titles` 并返回到章节 API
- 前端优先显示译本自带小标题，没有数据时再显示兜底标题

## V1.9.16 小标题兜底

目标：避免未覆盖精细段落标题的书卷完全不显示小标题。
已完成：

- 每一章第 1 节前都显示章节兜底标题
- 单章书信腓利门书、约翰二书、约翰三书、犹大书增加细分小标题
- 保留已覆盖章节的精细段落小标题

## V1.9.15 阅读定位与小标题

目标：修复切换章节后仍停留在上一章滚动位置的问题，并让经文阅读更接近常见圣经 App 的段落层次。
已完成：

- 上一章/下一章切换后自动回到章节开头
- 选择新书卷、新章节、切换主译本后从章节开头显示
- 前端增加段落小标题结构
- 先覆盖四福音书主要段落与创世记开篇段落，后续可继续扩展到全书

## V1.9.14 菜单与经文工具优化

目标：让手机端菜单更清爽，经文复制和多选不再依赖系统文本选择。
已完成：

- 主菜单拆为阅读、工具、数据、AI 分类，默认只展示常用阅读入口
- 经文长按/右键菜单增加应用内多选
- 多选复制支持带出处、只复制经文、合并成一段三种格式
- 书卷选择面板增加最近书卷过滤，并继续通过点击当前书卷名打开

## V1.9.13 APK 更新下载稳定性

目标：减少 Android APK 通过 GitHub 下载更新时因网络中断导致 0% 失败的问题。
已完成：

- APK 下载增加最多 3 次重试
- 支持 `Range` 续传已下载部分
- 下载完成后校验已下载字节数
- 下载失败时提示复制 APK 链接，用浏览器或下载器兜底

## V1.9.5 顶部书卷选择入口

目标：书卷/章节选择不再放在侧栏菜单里，改为点击顶部当前书卷名打开，贴近常见圣经阅读 App 的操作习惯。

已完成：

- Android 版本升级到 `1.9.5`
- 顶部当前书卷标题改为可点击按钮
- 书卷搜索、旧约/新约筛选、书卷网格、章节网格迁移到独立弹层
- 左侧菜单移除书卷/章节选择，保留版本、资源、AI 等辅助配置
- 点击章节后自动关闭书卷选择弹层并跳转
- Android 返回键/返回手势可关闭书卷选择弹层
